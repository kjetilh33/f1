apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

#namespace: default
commonLabels:
  app.kubernetes.io/name: f1-livetiming-test-bridge

resources:
  - f1-livetiming-test-bridge.deployment.yaml
  - f1-livetiming-test-bridge.service.yaml

# The generator will convert the application.yaml file into a K8 config map which can be mapped
# into the container
configMapGenerator:
  - name: f1-livetiming-test-bridge-config
    files:
      - application.yaml=application.yaml

generatorOptions:
  disableNameSuffixHash: true