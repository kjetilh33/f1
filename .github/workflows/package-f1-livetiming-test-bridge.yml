name: package-f1-livetiming-test-bridge
on:
  workflow_dispatch:
  push:
    branches:
      - "main"
    paths:
      - "f1-livetiming-test-bridge/**"
      
jobs:
  package-f1-livetiming-test-bridge:
    uses: "kjetilh33/utilities/.github/workflows/java-maven-skaffold-build-push-ghcr.yml@main"
    with:
      repositoryFolder: "./f1-livetiming-test-bridge/"
    secrets: inherit