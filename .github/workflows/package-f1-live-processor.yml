name: package-f1-live-processor
on:
  workflow_dispatch:
  push:
    branches:
      - "main"
    paths:
      - "f1-live-processor/**"
      
jobs:
  package-signalr-to-kafka:
    uses: "kjetilh33/utilities/.github/workflows/java-maven-skaffold-build-push-ghcr.yml@main"
    with:
      repositoryFolder: "./f1-live-processor/"
    secrets: inherit
