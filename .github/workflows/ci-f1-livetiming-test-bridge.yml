name: ci-f1-livetiming-test-bridge
on:
  workflow_dispatch:
  pull_request:
    paths:
      - "f1-livetiming-test-bridge/**"
      
jobs:
  build-f1-livetiming-test-bridge:
    uses: "kjetilh33/utilities/.github/workflows/java-maven-skaffold-build.yml@main"
    with:
      repositoryFolder: "./f1-livetiming-test-bridge/" 
